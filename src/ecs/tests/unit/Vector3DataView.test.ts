// const { testAdder } = require("../src/testy2");

import { context } from "../../../../tests/testAliases";

// it("adds 1 + 2 to equal 3", () => {
//   expect(1 + 2).toBe(3);
// });

// it("adds numbers", () => {
//   expect(testAdder(1, 2)).toBe(3);
// });

// TODO: tests, start doing them !!

describe("Vector3DataView", () => {
  context("123", () => {
    it("works", () => {
      expect(3 + 2).toEqual(5);
    });
  });
});
